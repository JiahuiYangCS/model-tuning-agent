# STSb Auto-Tune Agent v6 文档

欢迎使用本项目文档。此处为本仓库的快速导航页面。

- **Functions**：包含每个函数与模块的详细说明，见 `FUNCTIONS_DOC.md`。
- **Flowchart**：双语（中/英）流程图，见 `flowchart_bilingual.md`。

渲染提示：如果希望把 flowchart 导出为图片，请在有 Node.js 的环境中安装 `@mermaid-js/mermaid-cli`，然后运行 `mmdc` 将 Mermaid 文件导出为 SVG/PNG（示例见 `flowchart_bilingual.md`）。
